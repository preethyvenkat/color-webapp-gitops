
apiVersion: v1
kind: Service
metadata:
  name: color-webapp-preview
spec:
  selector:
    app: color-webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.port }}
