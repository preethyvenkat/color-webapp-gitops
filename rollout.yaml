apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: color-web
  namespace: default
spec:
  replicas: 1
  strategy:
    blueGreen:
      activeService: color-web-active
      previewService: color-web-preview
      autoPromotionEnabled: false
  selector:
    matchLabels:
      app: color-web
  template:
    metadata:
      labels:
        app: color-web
    spec:
      containers:
      - name: color-web
        image: preethyvenkat1122/color-webapp:v1
        ports:
        - containerPort: 80
        env:
        - name: COLOR
          value: "#FFBF00"

